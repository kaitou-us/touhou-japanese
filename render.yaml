services:
  - type: web
    name: touhou-japanese
    env: python
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    disk:
      name: data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
